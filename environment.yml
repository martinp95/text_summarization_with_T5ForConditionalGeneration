name: text_summarization_with_T5ForConditionalGeneration
channels:
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - torch==2.5
      - pytorch-lightning==2.5
      - transformers[sentencepiece]==4.47
      - datasets==3.2
      - ipywidgets
      - evaluate==0.4
      - streamlit==1.41